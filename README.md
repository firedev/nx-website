# nikolayx.com

Static one-page personal card. No build step — the repo root IS the site (`index.html` + assets).

Hosted on legacy GitHub Pages (branch `master`, path `/`), `CNAME` → nikolayx.com. Deploy = `git push`.

Note: the repo must stay **public** — a private repo on the free plan silently stops Pages builds while serving the stale site.
